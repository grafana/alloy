# Simple expressions
computed: expr((base + 100) * factor)
password: expr(env("PASSWORD"))
targets: expr(discovery.kubernetes.pods.targets)

# Multi-line expression should remain single-line in YAML
config: expr(array.concat([1, 2, 3], [4, 5, 6]))

