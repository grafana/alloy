loki.echo "debug" {}

loki.source.api "default" {
    http {
        listen_address = "0.0.0.0"
        listen_port = 7777
    }
    forward_to = [
        loki.echo.debug.receiver,
    ]
}


loki.source.api "other" {
    http {
        listen_address = "0.0.0.0"
        listen_port = 7778
    }
    forward_to = [
        loki.echo.debug.receiver,
    ]
}

