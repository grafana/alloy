# Install the task command from here: https://taskfile.dev
# It's a much more human-friendly version of Makefiles.

version: "3"

dotenv:
  - .env.credentials

vars:
  KUBECONFIG: build/kubeconfig.yaml
  CLUSTERNAME: alloy-example

tasks:
  cluster:create:
    desc: Create a kind cluster
    run: once
    status:
      - test -f {{.KUBECONFIG}} && kubectl --kubeconfig {{.KUBECONFIG}} cluster-info &>/dev/null
    cmds:
      - mkdir -p build
      - kind create cluster --config config/kind/multinode.yaml --name {{.CLUSTERNAME}} --kubeconfig {{.KUBECONFIG}}

  cluster:delete:
    desc: Delete the kind cluster
    run: once
    cmds:
      - kind delete cluster --name {{.CLUSTERNAME}}
      - rm -rf {{.KUBECONFIG}}

  update-helm-repos:
    desc: Update helm repositories
    run: once
    cmds:
      - helm repo add grafana https://grafana.github.io/helm-charts
      - helm repo add open-telemetry https://open-telemetry.github.io/opentelemetry-helm-charts
      - helm repo update

  deploy:alloy-helm:
    desc: Deploy local Grafana Alloy helm chart
    deps: [cluster:create]
    vars:
      ALLOY_VALUES_FILE: '{{.ALLOY_VALUES_FILE | default "config/alloy-helm/basic.yaml"}}'
      ALLOY_CHART_PATH: '{{.TASK_DIR}}/../../operations/helm/charts/alloy'
    preconditions:
      - test -f {{.ALLOY_VALUES_FILE}}
    cmds:
      - helm --kubeconfig {{.KUBECONFIG}} upgrade --install alloy {{.ALLOY_CHART_PATH}}
        -n monitoring --create-namespace
        -f {{.ALLOY_VALUES_FILE}}
        --wait

  deploy:cloud-onboarding:local:
    desc: Deploy Grafana Cloud Onboarding helm chart with shop Alloy config
    deps: [cluster:create, update-helm-repos]
    cmds:
      - helm --kubeconfig {{.KUBECONFIG}} upgrade --install grafana-cloud
        -n monitoring --create-namespace
        grafana/grafana-cloud-onboarding
        --set "cluster.name=example-kind-cluster"
        --set "grafanaCloud.fleetManagement.auth.password={{.GRAFANA_CLOUD_API_KEY}}"
        --set "grafanaCloud.fleetManagement.auth.username={{.REMOTE_CFG_USERNAME}}"
        --set "grafanaCloud.fleetManagement.url={{.REMOTE_CFG_URL}}"
        --set "collectors.alloy-daemon.alloy.extraEnv[0].name=GCLOUD_RW_API_KEY"
        --set "collectors.alloy-daemon.alloy.extraEnv[0].value={{.GRAFANA_CLOUD_API_KEY}}"
        -f config/shop/helm-values.yaml
        --wait
      - task: deploy:alloy-config

  deploy:alloy-config:
    desc: Create/update the Alloy shop ConfigMap from config.alloy and restart pods
    vars:
      ALLOY_CONFIG: config/shop/config.alloy
    preconditions:
      - test -f {{.ALLOY_CONFIG}}
    cmds:
      - |
        cat {{.ALLOY_CONFIG}} <(printf '\nlogging {\n\tlevel = "info"\n}\n\nlivedebugging {\n\tenabled = true\n}\n') > /tmp/alloy-shop-config.alloy
        HOSTNAME=dummy CLUSTER_NAME=dummy GCLOUD_RW_API_KEY=dummy NAMESPACE=monitoring POD_NAME=dummy alloy validate /tmp/alloy-shop-config.alloy
        echo "Config validated OK"
      - kubectl --kubeconfig {{.KUBECONFIG}} -n monitoring create configmap alloy-shop-config
        --from-file=config.alloy=/tmp/alloy-shop-config.alloy
        --dry-run=client -o yaml | kubectl --kubeconfig {{.KUBECONFIG}} apply -f -
      - kubectl --kubeconfig {{.KUBECONFIG}} -n monitoring delete pods -l app.kubernetes.io/name=alloy-daemon

  deploy:otelcol:
    desc: Deploy OpenTelemetry Collector helm chart using Alloy OTel Engine
    deps: [cluster:create, update-helm-repos]
    vars:
      OTELCOL_VALUES_FILE: '{{.OTELCOL_VALUES_FILE | default "config/otelcol-helm/values.yaml"}}'
    preconditions:
      - test -f {{.OTELCOL_VALUES_FILE}}
    cmds:
      - helm --kubeconfig {{.KUBECONFIG}} upgrade --install otelcol
        -n monitoring --create-namespace
        open-telemetry/opentelemetry-collector
        -f {{.OTELCOL_VALUES_FILE}}
        --wait

  remove:cloud-onboarding:local:
    desc: Remove Grafana Cloud Onboarding helm chart
    deps: [update-helm-repos]
    cmds:
      - helm --kubeconfig {{.KUBECONFIG}} uninstall grafana-cloud -n monitoring

  build:shop:
    desc: Build the shop application Docker image and load it into kind
    deps: [cluster:create]
    cmds:
      - docker build -t shop:latest {{.TASK_DIR}}/shop
      - kind load docker-image shop:latest --name {{.CLUSTERNAME}}

  deploy:shop:
    desc: Deploy the shop application to the kind cluster
    deps: [build:shop]
    cmds:
      - kubectl --kubeconfig {{.KUBECONFIG}} apply -f config/shop/manifests.yaml
      - kubectl --kubeconfig {{.KUBECONFIG}} -n shop-db rollout status deployment/shop-db --timeout=60s
      - kubectl --kubeconfig {{.KUBECONFIG}} -n shop-catalog rollout status deployment/shop-catalog --timeout=60s
      - kubectl --kubeconfig {{.KUBECONFIG}} -n shop-api rollout status deployment/shop-api --timeout=60s
      - kubectl --kubeconfig {{.KUBECONFIG}} -n shop-loadgen rollout status deployment/shop-loadgen --timeout=60s

  remove:shop:
    desc: Remove the shop application from the kind cluster
    cmds:
      - kubectl --kubeconfig {{.KUBECONFIG}} delete namespace shop-db shop-catalog shop-api shop-loadgen --ignore-not-found

  fault:oom:
    desc: Trigger a memory leak on one shop-db pod (OOM in ~5 minutes)
    cmds:
      - |
        POD=$(kubectl --kubeconfig {{.KUBECONFIG}} -n shop-db get pod -l app=shop-db -o jsonpath='{.items[0].metadata.name}')
        echo "Triggering OOM fault on $POD..."
        kubectl --kubeconfig {{.KUBECONFIG}} -n shop-db exec "$POD" -- wget -qO- --post-data '' http://localhost:8080/fault/oom
        echo ""
        echo "Memory leak started. Watch with: kubectl --kubeconfig {{.KUBECONFIG}} -n shop-db top pod"

  fault:deadlock:
    desc: Trigger a deadlock on one shop-catalog pod (stops responding)
    cmds:
      - |
        POD=$(kubectl --kubeconfig {{.KUBECONFIG}} -n shop-catalog get pod -l app=shop-catalog -o jsonpath='{.items[0].metadata.name}')
        echo "Triggering deadlock fault on $POD..."
        kubectl --kubeconfig {{.KUBECONFIG}} -n shop-catalog exec "$POD" -- wget -qO- --post-data '' http://localhost:8080/fault/deadlock
        echo ""
        echo "Deadlock active. Pod will stop responding to business requests (healthz/metrics still work)."

  run:pt-controller:
    desc: Run the Preemptive Telemetry controller locally
    cmds:
      - cd pt-controller && go run .

  alloy:ui:
    desc: Port-forward to Alloy and open its UI in the browser
    cmds:
      - |
        kubectl --kubeconfig {{.KUBECONFIG}} -n monitoring port-forward daemonset/grafana-cloud-alloy-daemon 12345:12345 &
        sleep 2
        open http://localhost:12345
        wait

  expose:
    desc: Expose all services on localhost via port-forward
    deps: [deploy:cloud-onboarding:local]
    cmds:
      - "{{.TASK_DIR}}/config/expose.sh {{.KUBECONFIG}}"
