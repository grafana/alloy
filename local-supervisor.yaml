server:
  endpoint: "${env:GCLOUD_FM_URL}/v1/opamp"
  headers:
    Authorization: "Basic ${env:GCLOUD_BASIC_AUTH}"

capabilities:
  accepts_remote_config: true
  reports_remote_config: true

agent:
  executable: ./build/alloy
  args: 
    - otel
    - --feature-gates
    - service.AllowNoPipelines

storage:
  directory: ./storage
