container:
  privileged: true
  cap_add:
    - "SYS_ADMIN"
    - "SYS_PTRACE"
    - "SYS_RESOURCE"
  security_opt:
    - "apparmor:unconfined"
  pid_mode: "host"
  # tetragon_image: quay.io/cilium/tetragon:v1.6.0
  tetragon_image: cilium/tetragon:latest
