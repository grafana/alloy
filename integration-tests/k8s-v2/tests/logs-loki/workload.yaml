apiVersion: v1
kind: Namespace
metadata:
  name: ${TEST_NAMESPACE}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: logs-source
  namespace: ${TEST_NAMESPACE}
data:
  app.log: |
    log-line test_id=${TEST_ID}: this line should arrive in Loki
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: log-generator
  namespace: ${TEST_NAMESPACE}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: log-generator
      test-id: ${TEST_ID}
  template:
    metadata:
      labels:
        app: log-generator
        test-id: ${TEST_ID}
    spec:
      containers:
        - name: writer
          image: busybox:1.36
          args:
            - "sh"
            - "-c"
            - "cat /input/app.log > /var/log/k8s-v2/app.log && sleep 600"
          volumeMounts:
            - name: input
              mountPath: /input
            - name: logs
              mountPath: /var/log/k8s-v2
      volumes:
        - name: input
          configMap:
            name: logs-source
        - name: logs
          emptyDir: {}
