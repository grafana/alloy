#!/usr/bin/env bash
#MISE hide=true

set -euo pipefail

while IFS= read -r mod; do
  dir="$(dirname "$mod")"
  echo "==> golangci-lint: $dir"
  (cd "$dir" && golangci-lint run -v --timeout=10m)
done < <(find . -name go.mod -not -path '*/testdata/*')
