When a component has only expression-based endpoints (unresolvable statically),
no endpoint section is generated because there are no known static URLs.

-- config.alloy --
loki.write "logs" {
  endpoint {
    url = local.file.creds.content
  }
}

-- expected_policy.yaml --
components:
    mode: allowlist
    list:
        - loki.write
