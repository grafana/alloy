prometheus.scrape has dynamic endpoints (discovery targets) — no endpoint section generated.

-- config.alloy --
prometheus.scrape "default" {
  targets    = []
  forward_to = []
}

-- expected_policy.yaml --
components:
    mode: allowlist
    list:
        - prometheus.scrape

-- expected_dynamic_components --
prometheus.scrape
