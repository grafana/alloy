# mappings.json — transformation examples

Auto-generated by `go run ./gen/main.go`. DO NOT EDIT.

Each section shows: the Beyla YAML format, the Alloy input form that produces it.

---

## inject_wrappers

### `ebpf.payload_extraction.http` wraps `openai`, `anthropic`, `gemini`, `bedrock` under `genai`

Beyla YAML:

```yaml
ebpf:
  payload_extraction:
    http:
      genai:
        openai:
          enabled: true
```

Alloy:

```
ebpf {
  payload_extraction {
    http {
      openai {
        enabled = true
      }
    }
  }
}
```

## aliases

### `filter` → `filters`

Beyla YAML:

```yaml
filter:
  - example_value
```

Alloy (canonical):

```
filter {
  filter = ["example_value"]
}
```

Alloy (compat):

```
filter {
  filters = ["example_value"]
}
```

### `prometheus_export.extra_resource_attributes` → `extra_resource_labels` (warn: true)

Beyla YAML:

```yaml
prometheus_export:
  extra_resource_attributes:
    - example_value
```

Alloy (canonical):

```
prometheus_export {
  extra_resource_attributes = ["example_value"]
}
```

Alloy (compat, emits deprecation warning):

```
prometheus_export {
  extra_resource_labels = ["example_value"]
}
```

### `prometheus_export.extra_span_resource_attributes` → `extra_span_resource_labels` (warn: true)

Beyla YAML:

```yaml
prometheus_export:
  extra_span_resource_attributes:
    - example_value
```

Alloy (canonical):

```
prometheus_export {
  extra_span_resource_attributes = ["example_value"]
}
```

Alloy (compat, emits deprecation warning):

```
prometheus_export {
  extra_span_resource_labels = ["example_value"]
}
```

## skip

Schema fields suppressed from TODO generation. Each is handled manually with a different alloy key.

| Schema path | Reason |
|---|---|
| `injector.resources.addK8sUIDAttributes` | `addK8sUIDAttributes` has no direct alloy mapping; handled by hand-written code |
| `injector.resources.resourceAttributes` | `resourceAttributes` has no direct alloy mapping; handled by hand-written code |
| `injector.resources.useLabelsForResourceAttributes` | `useLabelsForResourceAttributes` has no direct alloy mapping; handled by hand-written code |
| `internal_metrics.prometheus` | `prometheus` has no direct alloy mapping; handled by hand-written code |

## manual_sections

Top-level YAML sections absent from `schema.json`; config emission is hand-written.

- `traces`
- `attributes`
- `filter`

## multi_section

One Alloy block maps to multiple Beyla YAML sections. Fill helpers are generated.

| Alloy path | Beyla YAML section | Fill helper |
|---|---|---|
| `metrics` | `prometheus_export` | `fillPrometheusExportConfig` |
| `metrics.network` | `network` | `fillNetworkConfig` |

## flatten_transforms

Schema fields with a common prefix are grouped into a nested Alloy block.

### `GlobAttributes`: `k8s_*` → `kubernetes {}`

Schema fields prefixed `k8s_` (e.g. `k8s_namespace`) are exposed as `kubernetes { namespace = ... }`.

## map_keyed_by

A YAML map keyed by a named field is represented as a repeated block in Alloy.

| Go type | Key field | Fill helper |
|---|---|---|
| `AttributeFamilies` | `attr` | `fillAttributeFamiliesConfig` |
| `Selections` | `attr` | `fillSelectionsConfig` |

## name_overrides

Schema `$def` names mapped to custom Go type names.

| Schema def | Go type |
|---|---|
| `BeylaDiscoveryConfig` | `Discovery` |
| `EBPFTracer` | `EBPF` |
| `HTTPConfig` | `HTTPPayloadExtraction` |
| `InternalMetricsConfig` | `InternalMetrics` |
| `MapsConfig` | `EBPFMapsConfig` |
| `PayloadExtraction` | `PayloadExtraction` |
| `RoutesConfig` | `Routes` |
| `SDKExport` | `InjectorSDKExport` |
| `SDKInject` | `Injector` |
| `SDKResource` | `InjectorSDKResource` |
| `StatsConfig` | `Stats` |
| `WebhookConfig` | `InjectorWebhook` |

